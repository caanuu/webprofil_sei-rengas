[phases.setup]
nixPkgs = ["nodejs-22_x", "php83", "composer"]

[phases.install]
cmds = [
  "rm -rf node_modules package-lock.json",
  "npm install --include=optional"
]

[phases.build]
cmds = [
  "npm run build"
]

[start]
cmd = "php -S 0.0.0.0:$PORT -t public"
